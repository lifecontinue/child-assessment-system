# 小学二年级教师端评价工具

基于 Next.js 14 + Supabase + Vercel 构建的轻量化评价系统。

## ✨ 功能特性

- 📚 **五大评价领域**：学科素养、品德发展、身心健康、审美素养、劳动实践
- 📊 **实时数据可视化**：雷达图、趋势分析、班级整体水平
- 🎯 **28个教学节点**：覆盖二年级上册全部教学进度
- ⭐ **自动评级系统**：基于定量数据自动计算星级
- 📝 **一键生成报告**：学期初诊断、学期中发展、学期末总结
- 🔥 **课堂工具集**：发言热力图、作业显微镜、小组合作记录

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

创建 `.env.local` 文件：

```env
NEXT_PUBLIC_SUPABASE_URL=your_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_key
```

### 3. 初始化数据库

在 Supabase SQL Editor 中依次执行：

1. `supabase/schema-merge.sql` - 创建表结构（兼容现有 students 表）
2. `supabase/seed-merge.sql` - 插入预置数据

### 4. 运行开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000)

## 📁 项目结构

```
teacher-evaluation-tool/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/               # API 路由
│   │   ├── page.tsx           # 首页 Dashboard
│   │   ├── subject-assessment/ # 学科评价
│   │   ├── comprehensive-assessment/ # 综合素质
│   │   ├── tools/             # 评价工具
│   │   ├── reports/           # 评价报告
│   │   ├── data-viz/          # 数据分析
│   │   ├── students/          # 学生管理
│   │   └── heatmap/           # 发言热力图
│   ├── components/            # React 组件
│   │   ├── Dashboard.tsx      # 主控台
│   │   ├── SpeakingHeatmap.tsx # 发言热力图
│   │   ├── EvaluationForm.tsx # 评价表单
│   │   ├── RadarChart.tsx    # 雷达图
│   │   ├── Sidebar.tsx        # 侧边栏
│   │   └── Header.tsx         # 顶部导航
│   └── lib/                   # 工具函数
│       ├── supabase.ts        # Supabase 客户端
│       ├── constants.ts       # 常量定义
│       └── utils.ts           # 工具函数
├── supabase/
│   ├── schema-merge.sql       # 数据库结构（合并版本）
│   ├── seed-merge.sql         # 预置数据
│   └── functions/             # Edge Functions
│       ├── auto-rating/       # 自动评级函数
│       └── generate-report/   # 报告生成函数
└── vercel.json                # Vercel 配置
```

## 🗄️ 数据库表结构

### 核心表

- **students** - 学生表（兼容儿童测评系统）
- **teaching_progress** - 教学进度表
- **evaluation_records** - 评价记录表
- **teacher_profiles** - 教师表
- **reports** - 报告表

### 表关联

- `evaluation_records.student_id` → `students.id` (BIGINT)
- `reports.student_id` → `students.id` (BIGINT)
- `teacher_profiles.id` → `auth.users.id` (UUID)

## 🔧 技术栈

- **前端**：Next.js 14 (App Router) + TypeScript + Tailwind CSS
- **后端**：Supabase (PostgreSQL + Auth + Storage + Edge Functions)
- **部署**：Vercel
- **图表**：Recharts
- **PDF生成**：html2pdf.js

## 📝 核心功能

### Dashboard 主控台
- 今日评价任务
- 班级整体水平雷达图
- 关注学生列表
- 最近评价记录
- 评价进度概览

### 学科评价
- 语文、数学、英语三个学科
- 支持多指标评价
- 星级评价（1-3星）
- 评价记录查询

### 综合素质评价
- 品德发展、身心健康、审美素养、劳动实践
- 多维度评价体系
- 评价记录管理

### 课堂工具
- 发言热力图（6x8座位网格）
- 作业显微镜评价表
- 小组合作贡献值

### 报告生成
- 学期初诊断报告
- 学期中发展报告
- 学期末总结报告
- PDF 导出功能

## 🧪 测试

参考 `TESTING.md` 进行功能测试和数据库关联测试。

## 📚 相关文档

- [TESTING.md](./TESTING.md) - 测试指南
- [supabase/schema-merge.sql](./supabase/schema-merge.sql) - 数据库结构
- [supabase/seed-merge.sql](./supabase/seed-merge.sql) - 预置数据

## 🚀 部署到 Vercel

1. 推送代码到 GitHub
2. 在 Vercel 导入项目
3. 配置环境变量
4. 自动部署完成

## 📄 许可证

MIT
